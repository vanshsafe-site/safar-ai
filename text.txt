# Initialize git if not already
git init

# Add all files
git add .

# Commit
git commit -m "Initial commit - Safar AI tourism project"

# Connect your repo
git remote add origin https://github.com/vanshsafe-site/safar-ai.git

# Make sure you're on main branch
git branch -M main

# Push to GitHub
git push -u origin main
